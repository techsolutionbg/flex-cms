# Extension API v1

This namespace is intentionally empty until the plugin and theme lifecycle is implemented. Future extension-facing contracts will be introduced here with semantic versioning and compatibility tests; internal platform contracts must not be exposed as extension API by accident.
