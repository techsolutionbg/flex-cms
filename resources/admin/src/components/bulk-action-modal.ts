export function bulkActionModalMarkup(actionDropdown: string): string {
  return `<div class="modal-backdrop" x-show="bulkModal.open" x-cloak @click.self="closeBulkModal" @keydown.escape.window="closeBulkModal"><section class="modal bulk-action-modal" role="dialog" aria-modal="true" aria-labelledby="bulk-action-modal-title"><header class="modal-header"><h2 id="bulk-action-modal-title">Масови действия</h2><button class="modal-close" type="button" aria-label="Затвори" @click="closeBulkModal">×</button></header><div class="modal-content modal-filter-content"><p>Изберете операция за маркираните страници.</p><p class="muted" x-text="\`Избрани записи: \${selectedPageIds.length}\`"></p><label><span class="field-label">Действие</span>${actionDropdown}</label><div class="bulk-settings-fields" x-show="bulkModal.action === 'settings'"><label class="checkbox-field"><input type="checkbox" x-model="bulkModal.settings.use_parent_slugs"><span>Използвай slug-овете на родителските страници</span></label><label class="checkbox-field"><input type="checkbox" x-model="bulkModal.settings.no_index"><span>Не показвай страниците в търсачки</span></label><label class="checkbox-field"><input type="checkbox" x-model="bulkModal.settings.show_in_navigation"><span>Показвай страниците в навигацията</span></label><label class="checkbox-field"><input type="checkbox" x-model="bulkModal.settings.show_in_sitemap"><span>Включи страниците в sitemap-а</span></label></div></div><footer class="modal-footer"><button class="button secondary" type="button" @click="closeBulkModal">Отказ</button><button class="button primary" type="button" :disabled="bulkModal.busy || selectedPageIds.length === 0" @click="applyBulkAction" x-text="bulkModal.busy ? 'Изпълнение…' : 'Приложи'"></button></footer></section></div>`
}
