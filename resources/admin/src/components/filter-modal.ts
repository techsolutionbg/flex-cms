export function filterModalMarkup(statusDropdown: string, structureDropdown: string, viewDropdown: string): string {
  return `<div class="modal-backdrop" x-show="filterModalOpen" x-cloak @click.self="closeFilterModal" @keydown.escape.window="closeFilterModal"><section class="modal filter-modal" role="dialog" aria-modal="true" aria-labelledby="page-filter-modal-title"><header class="modal-header"><h2 id="page-filter-modal-title">Филтри на страниците</h2><button class="modal-close" type="button" aria-label="Затвори" @click="closeFilterModal">×</button></header><div class="modal-content modal-filter-content"><label><span class="field-label">Състояние</span>${viewDropdown}</label><label><span class="field-label">Статус</span>${statusDropdown}</label><label><span class="field-label">Структура</span>${structureDropdown}</label></div><footer class="modal-footer"><button class="button secondary" type="button" @click="resetPageFilters">Изчисти</button><button class="button secondary" type="button" @click="closeFilterModal">Отказ</button><button class="button primary" type="button" @click="applyPageFilters">Приложи филтрите</button></footer></section></div>`
}
